# colcon workspace build artifacts (built inside the container, not host-mounted,
# but ignore here too in case you ever build locally)
build/
install/
log/

# Python / editor / OS cruft
**/__pycache__/
*.pyc
.DS_Store
.vscode/

# Flattened URDF + copied meshes generated for Blender import
**/blender_export/

# Repositories managed by vcstool (see repos.yaml)
/src/robomaster_ros/
/src/urdf_tutorial/

# Claude Code local, machine-specific settings
.claude/settings.local.json
